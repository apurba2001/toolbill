<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Asked for after the first subscription is added, never on launch. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      Exchange rates only. The request carries currency codes and nothing else: no identifier,
      no subscription, no amount. Everything the user enters stays on the device.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Alarms do not survive a reboot; this is how they are put back. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--
      Deliberately absent: SCHEDULE_EXACT_ALARM / USE_EXACT_ALARM. A renewal warning three days
      out does not need to land on a given second, and Play restricts exact alarms to alarm
      clocks and calendars. Reminders use AlarmManager.setWindow with an hour of tolerance.
    -->

    <application
        android:name=".ToolbillApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Toolbill">

        <!-- singleTop so the launcher shortcut and the quick-settings tile deliver their
             shortcut_action through onNewIntent instead of rebuilding the activity. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <receiver
            android:name=".widget.ToolbillWidgetReceiver"
            android:label="@string/widget_name"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/toolbill_widget_info" />
        </receiver>

        <!--
          Hands a generated CSV to the share sheet. Grants are per-URI and temporary, and the
          shared directory holds only staged exports; see res/xml/export_paths.xml.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.export"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/export_paths" />
        </provider>

        <!-- The one alarm the portfolio holds. Not exported: only this app sets it. -->
        <receiver
            android:name=".core.reminder.ReminderReceiver"
            android:exported="false" />

        <!--
          Exported because every action below is a protected system broadcast: the platform is
          the only sender, so there is nothing for another app to spoof here.
        -->
        <receiver
            android:name=".core.reminder.RescheduleReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".AddSubscriptionTileService"
            android:exported="true"
            android:icon="@drawable/ic_add"
            android:label="@string/tile_add_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>

</manifest>
